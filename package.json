{
	"scripts": {
		"build": "esbuild ./demo-src/main.js --bundle --minify --sourcemap --format=esm --platform=node --outdir=./demo-build"
	},
	"dependencies": {
		"esbuild": "^0.25.2",
		"three": "^0.175.0"
	}
}
